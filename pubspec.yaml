name: flutter_login
description: A simple flutter login using firebase.
version: 1.0.0+1

publish_to: none

environment:
  sdk: ^2.18.6

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
